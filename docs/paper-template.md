---
# Copy this file into src/content/papers/ and rename it.
# Filename convention: <year>-<first-author-surname>-<short-slug>.md
# The filename becomes the URL: /papers/2025-doe-llm-interviewers

title: "Full paper title, in sentence case as published"
authors: ["Given Surname", "Given Surname"]
date: 2025-04-17 # YYYY-MM-DD, or YYYY-MM if the day is unknown
url: "https://arxiv.org/abs/2504.01234" # required — the landing page, not the PDF

# Optional identifiers. `arxiv` alone is enough for `npm run thumbs` to make a thumbnail.
arxiv: "2504.01234"
doi: "10.1145/3706598.3713234"
pdfUrl: # only needed when there is no arxiv id and the PDF is openly reachable

venue: "CHI 2025" # omit entirely for an unpublished preprint
venueType: conference # conference | journal | workshop | preprint | thesis | tech-report

# --- Classification. Leave blank rather than guessing; blank renders as an
# --- unfilled form field, which is a designed state, not a bug.
autonomy: # autonomous | ai-assisted | both | not-applicable
modality: # text | voice | multimodal | not-applicable
paperType: [] # any of: new-system, exploratory-position, evaluation-benchmark, survey, dataset
domains: [] # free tags, lowercased automatically: ["health", "market research"]

code_url:
dataset_url:

thumbnail: # generated by `npm run thumbs`; leave blank
bibtex: |
  @inproceedings{surname2025short,
    title     = {...},
    author    = {...},
    booktitle = {...},
    year      = {2025}
  }

# Field names a human has not yet verified. Surfaced by `npm run review`.
needsReview: ["autonomy", "modality", "paperType", "domains"]

# Set true when the publisher's abstract cannot be reproduced and the body below
# is our own summary instead.
abstractIsSummary: false

draft: false
---

The abstract goes here, as the body of the file. Plain prose; Markdown is allowed.
